# minecraft-trend-bot 運用引継ぎガイド

このドキュメントは、本アプリ(minecraft-trend-bot)の管理者を引き継ぐ際に必要な手順と、引継ぎ後の定常運用でやるべきことをまとめたものです。

セットアップの技術的な詳細(APIキーの取得方法など)は [README.md](README.md) に記載されているため、本ドキュメントでは「誰が何の権限を持っていて、それを誰にどう引き渡すか」に焦点を当てています。

---

## 1. このアプリの概要

- **やっていること**: YouTube上のマインクラフト関連動画を毎週自動収集し、ランキング・傾向分析をMicrosoft Teamsに投稿。分析結果はSharePointリストとSupabase(Web公開ダッシュボード)にも履歴保存される。
- **実行タイミング**: 毎週月曜9:00(Asia/Tokyo)。外部cronサービス(cron-job.org)が起点となり、GitHub Actionsのワークフローを呼び出す。
- **公開ダッシュボード**: https://bookbear-daisho.github.io/minecraft-trend-bot/
- **処理フロー・ディレクトリ構成**: [README.md](README.md) の「処理の流れ」「ディレクトリ構成」を参照。

このアプリは以下の**6つの外部サービス/アカウント**に依存しています。引継ぎでは、それぞれについて「新しい管理者がアクセス・変更できる状態」にする必要があります。

| # | サービス | 用途 | 依存度 |
|---|---|---|---|
| 1 | GitHubリポジトリ | コード管理・GitHub Actions実行・GitHub Pages公開 | 必須 |
| 2 | cron-job.org | 週次実行のトリガー | 必須(止まると通知が来なくなる) |
| 3 | Google Cloud(YouTube Data API) | 動画データ取得 | 必須 |
| 4 | Power Automate(または Teams Incoming Webhook) | Teams通知の投稿先 | 必須 |
| 5 | Azure AD アプリ登録 + SharePoint | 分析履歴の保存先(社内向け) | 任意(`SHAREPOINT_ENABLED=true`の場合のみ) |
| 6 | Supabase | 分析履歴の保存先(Web公開ダッシュボード用) | 任意(`SUPABASE_ENABLED=true`の場合のみ) |

現状は5・6とも有効(`.github/workflows/weekly.yml`参照)のため、実質すべて必須と考えてください。

---

## 2. 引継ぎ手順チェックリスト

以下、サービスごとに「現在の所有者」「引継ぎ方法」を記入・実施してください。**現在の所有者欄は本ドキュメント作成時点では空欄にしています。引継ぎ担当者が実際のアカウント情報を確認して記入してください。**

### 2-1. GitHubリポジトリ

- リポジトリ: `bookbear-daisho/minecraft-trend-bot`
- 現在の所有者/Owner権限者: `___________`
- 手順:
  1. リポジトリの Settings → Collaborators and teams から新管理者を **Admin権限** で追加する。
  2. 新管理者がSecretsを確認・更新できることを確認する(Settings → Secrets and variables → Actions)。登録されているSecrets:
     - `YOUTUBE_API_KEY`
     - `TEAMS_WEBHOOK_URL`
     - `SHAREPOINT_TENANT_ID` / `SHAREPOINT_CLIENT_ID` / `SHAREPOINT_CLIENT_SECRET` / `SHAREPOINT_SITE_HOSTNAME` / `SHAREPOINT_SITE_PATH` / `SHAREPOINT_DASHBOARD_URL`
     - `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`
     - ⚠️ `SUPABASE_URL`・`SUPABASE_SERVICE_ROLE_KEY`はREADME.mdの「事前準備(Secrets登録)」リストに記載漏れがあります(後日追加を推奨)。実際は`.github/workflows/weekly.yml`で参照されているため必須です。
  3. GitHub Pages公開設定(Settings → Pages)が `docs/` フォルダから公開されていることを確認する(通常は変更不要)。
  4. 元の管理者をCollaboratorから外す場合は、新管理者への引継ぎ完了(週次実行が正常に動くこと)を確認した後に行う。

### 2-2. cron-job.org(週次トリガー)

- ジョブ内容: `https://api.github.com/repos/bookbear-daisho/minecraft-trend-bot/actions/workflows/weekly.yml/dispatches` へ、毎週月曜9:00にPOST(Authorizationヘッダーに GitHub PAT)
- 現在のアカウント: `___________`
- 手順:
  1. cron-job.orgのアカウントを新管理者に共有する、または新管理者名義で新規アカウントを作成しジョブを再作成する。
  2. **重要**: 現在ジョブに設定されているGitHub PAT(Personal Access Token)は、発行した人のGitHubアカウントに紐づいています。元の管理者のアカウントが将来使えなくなると、このPATも失効し週次実行が止まります。引継ぎ時に、**新管理者のGitHubアカウントで新しいPATを発行し直し**、cron-job.orgのジョブ設定のAuthorizationヘッダーを更新することを強く推奨します(PAT発行手順はREADME.md「6-1」参照)。
  3. ジョブ設定変更後、Actionsタブから手動実行(`Run workflow`)して動作確認する。

### 2-3. Google Cloud / YouTube Data API キー

- 現在のプロジェクト: `___________`
- 現在の管理者: `___________`
- 手順:
  1. Google Cloud Consoleで、新管理者をプロジェクトの共同編集者/オーナーとして追加する(IAM & Admin)。
  2. 可能であれば、新管理者がキーの発行・失効を自分で行えるようにしておく(APIキー自体は使い回してよいが、緊急時に無効化できる権限が必要)。
  3. クォータ状況(1日10,000ユニット、`search.list`は1回100ユニット消費)を新管理者に共有する。

### 2-4. Power Automate / Teams Webhook

- 現在の投稿モード: `TEAMS_POST_MODE=powerAutomate`(`.github/workflows/weekly.yml`参照)
- フロー所有者: `___________`
- 手順:
  1. **重要**: Power Automateのフローは基本的に個人のMicrosoft 365アカウント上に作成されます。そのアカウントが無効化・退職処理されるとフローも停止し、Teams通知が止まります。
  2. Power Automate(make.powerautomate.com)→ 対象のフローを開き「共有」から新管理者を**共同所有者**として追加する。
  3. 共同所有者への変更が難しい場合は、新管理者名義で同じ内容のフローを作り直し、発行されるHTTP POST URLを GitHub Secrets の `TEAMS_WEBHOOK_URL` に更新する(手順はREADME.md「2. Teams Webhook URLの設定方法」参照)。

### 2-5. Azure AD アプリ登録 + SharePoint

- アプリ登録名: `___________`(README.mdでは例として`minecraft-trend-bot`)
- テナント管理者: `___________`
- 手順:
  1. Azure Portal → Azure Active Directory → アプリの登録 から対象アプリを開き、「所有者」に新管理者を追加する。
  2. 「証明書とシークレット」で、現在のクライアントシークレットの**有効期限**を確認する。期限切れが近い場合は引継ぎ時に併せて更新し、GitHub Secretsの`SHAREPOINT_CLIENT_SECRET`も更新する。
  3. SharePointリスト(`MinecraftTrendHistory`)・分析ダッシュボードページへのアクセス権を新管理者に付与する。
  4. テナント管理者の同意(`Sites.ReadWrite.All`権限)がテナント全体で必要になるため、新管理者がテナント管理者でない場合は情シス担当への連絡窓口も引継ぎ資料に記載しておく。

### 2-6. Supabase

- プロジェクト名: `___________`
- 現在のオーナー: `___________`
- 手順:
  1. Supabase管理画面(Project Settings → Team)から新管理者をメンバーとして招待する。
  2. `SUPABASE_SERVICE_ROLE_KEY`(GitHub Secretsに登録済み)は強い権限を持つため、引継ぎ後に不要なアクセス元がないか確認し、必要であればキーをローテーション(再発行)してGitHub Secretsを更新する。
  3. `docs/index.html`内にハードコードされている`SUPABASE_URL`・`SUPABASE_KEY`(publishable/anon key)は公開情報として問題ないが、テーブルのRLS(Row Level Security)設定で書き込みができないようになっていることを新管理者に説明しておく。

### 2-7. GitHub Pages(公開ダッシュボード)

- URL: https://bookbear-daisho.github.io/minecraft-trend-bot/
- 特別な引継ぎ作業は不要(2-1のリポジトリアクセス権があれば管理可能)。`docs/index.html`を編集してpushすれば数分で反映される。

---

## 3. 引継ぎ後の定常運用チェックリスト

### 毎週(月曜9:00以降)
- [ ] Teamsに通知が届いているか確認する。
- [ ] 届いていない場合は、以下の順で確認する:
  1. cron-job.orgのジョブ実行履歴(実行されたか、PATエラーが出ていないか)
  2. GitHubリポジトリのActionsタブ(ワークフローが起動し正常終了しているか)
  3. Power Automateのフロー実行履歴(HTTPトリガーは受信できているか)

### 月1回程度
- [ ] GitHub Actionsの実行履歴を見て、エラーや警告が出ていないか確認する。
- [ ] Google Cloud ConsoleでYouTube Data APIのクォータ使用状況を確認する(クォータ超過エラーが出た場合は`src/config.ts`の`SEARCH_QUERIES`を減らす)。
- [ ] 公開ダッシュボード(https://bookbear-daisho.github.io/minecraft-trend-bot/ )が正常に表示されるか確認する(ヘッダーの「データ鮮度バッジ」が🟢になっているか)。

### 期限管理(年1回程度、または通知が来たら)
- [ ] Azure ADアプリ登録のクライアントシークレットの有効期限を確認し、切れる前に更新する(切れるとSharePoint連携が失敗し、Teams通知にエラーが投稿される)。
- [ ] cron-job.orgに登録したGitHub PATの有効期限(Fine-grained tokenは作成時に期限を設定できる)を確認し、切れる前に再発行する。

---

## 4. トラブルシューティング早見表

| 症状 | 確認するところ |
|---|---|
| Teams通知が来ない | cron-job.org実行履歴 → GitHub Actions実行履歴 → Power Automate実行履歴 の順に確認 |
| Teams通知に「バッチ処理中にエラーが発生しました」と投稿される | GitHub Actionsのログ(`npm start`のステップ)を開き、スタックトレースを確認 |
| SharePointにデータが増えない | Azure ADクライアントシークレットの有効期限、`SHAREPOINT_ENABLED`設定、Graph APIの権限同意状況を確認 |
| Web公開ダッシュボードが更新されない | Supabaseの`SUPABASE_SERVICE_ROLE_KEY`の有効性、GitHub Actionsの実行結果を確認 |
| YouTube動画が全然取得できない/減った | Google CloudのAPIクォータ超過、YouTube Data API自体の有効化状態を確認 |

---

## 5. 参照ドキュメント

- [README.md](README.md) — 各外部サービスの初期セットアップ手順(APIキー取得、SharePoint列作成など)の詳細
- [notes/](notes/) フォルダ — これまでの機能追加・仕様変更の経緯と判断理由(AIタスクレビュー形式)。特に直近の変更内容を把握したい場合は日付の新しいものから読むとよい

---

## 管理情報

|項目|内容|
|---|---|
|引継ぎ元|`___________`|
|引継ぎ先|`___________`|
|引継ぎ実施日|`___________`|
|本ドキュメント最終更新日|2026-07-31|
